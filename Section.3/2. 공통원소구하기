package main;

import java.util.*;

class Main {

    public static void main(String[] args) {
        Scanner sh = new Scanner(System.in);

        int num = sh.nextInt();
        int[] arr = new int[num];
        for (int i = 0; i < num; i++) {
            arr[i] = sh.nextInt();
        }

        int num1 = sh.nextInt();
        int[] arr1 = new int [num1];
        for (int i = 0; i < num1; i++) {
            arr1[i] = sh.nextInt();
        }
        List<Integer> list = new ArrayList<>();

        Arrays.sort(arr);
        Arrays.sort(arr1);

        int n = 0, nn =0;
       while (n<num && nn<num1 ) {
           if (arr[n] == arr1[nn]) {
               list.add(arr[n++]);
               nn++;
           } else if (arr[n] < arr1[nn]) {
               n++;
           } else {
               nn++;
           }
       }
           for (Integer integer : list){
               System.out.print(integer + " ");
           }
       }
    }
